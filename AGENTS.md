---
rule_id: agents-root
version: 1.81.0
last_updated: 2026-09-22
dependencies: []
---

# AGENTS.md

## 目标
- `AGENTS.md`：只保留全局硬约束、阶段门控和按需导航；协作流程见 `collaboration-AGENTS.md`
- `frontend-AGENTS.md`：页面、布局、组件、样式、交互与前端测试
- `doc-AGENTS.md`：设计文档、设计系统
- `design-system.yaml`：设计 token 与 UI 规范（内置基线，按产品品牌定制后生效）
- `TASK.md`：唯一活动任务记录文件

---

## 最小充分上下文
- 简单问题（纯问答、单文件、不影响业务逻辑、不新增依赖）与单点低风险改动（单文件、不影响核心链路、不涉及权限 / 缓存 / 迁移 / 发布）：只读命中的子规则和目标文件，不默认预读全部子规则
- 只有在需要实际落文件、任务跨设计、页面实现与交付验证、涉及数据库迁移、缓存策略变更、鉴权模型变更、发布链路调整、环境变量变更等高风险内容、或当前信息不足以判断影响范围时，才继续补读更多规则或实现文件
- 任务升级时按命中场景逐步补充上下文，而不是一开始加载全部信息

---

## 根文件原则
- 根 `AGENTS.md` 只放所有任务都需要知道的规则，不重复展开前端与文档细节
- 用户指令始终高于本文件
- 不盲从用户前提：发现更优思路、遗漏或矛盾时先说明理由与影响，再按用户最终决定执行；细则见 `collaboration-AGENTS.md`「坦率反馈与过程沟通」
- 先理解业务目标与现有实现，再修改代码或规则；优先复用既有结构，避免无关重构和过度设计
- 代码先服务于人，再服务于机器：方案先按 KISS 收敛，核心逻辑优先直白写法，复杂分支用行内注释说明“为什么”
- 优先做可验证的改动；无法验证时必须说明原因、影响和建议下一步

---

## 子规则索引

| 文件 | 适用范围 | 何时阅读 |
| --- | --- | --- |
| `frontend-AGENTS.md` | 页面、布局、交互、样式、前端 smoke test | 涉及静态页实现与交互时 |
| `doc-AGENTS.md` | 设计文档、`design-system.yaml` | 涉及设计说明、规则维护时 |

- 发布或高风险改动：至少同时读当前文件和所有命中的子规则
- 小规模规则修订或文案调整：命中后只读对应子规则的相关段落，不必通读全文

### 按需阅读顺序
1. 先读当前文件，确认全局规则、任务阶段和交付要求。
2. 根据任务类型命中对应子规则，不预读全部文件。
3. 涉及界面时，在命中 `frontend-AGENTS.md` 后再读 `design-system.yaml`；`design-system.yaml` 中的读取建议不得覆盖本文件定义的根规则优先级。
4. 再阅读相关页面与资源文件，确认复用点后再动手。

---

## 开发阶段

### 三个阶段

```text
阶段 0          阶段 1                阶段 2
设计文档   →   页面与交互实现     →   验证与交付
（用户确认）    （页面实现角色）        （交付验证角色）
```

- 新功能需经过完整阶段流程，以下场景可简化或跳过阶段 0：
  - Bug 修复
  - 已有页面的样式或文案微调
  - 资源引用、构建配置或部署说明调整
- 除上述场景外，各阶段必须满足入场条件后方可进入下一阶段
- 阶段括号仅表示推荐职责角色，不绑定具体 AI 工具；单 Agent 场景下可在满足门控条件后连续执行各阶段

### 阶段职责
- 阶段 0：需求整理、设计文档与页面说明，细则见 `doc-AGENTS.md`
- 阶段 1：页面骨架、交互、视觉落地与前端验证，细则见 `frontend-AGENTS.md`
- 阶段 2：构建、部署说明与交付验证，结合当前文件与命中的子规则执行

---

## 全局工作规则
- 实际修改前，确认任务类型、命中子规则、目标实现、复用点、影响范围、验证路径和待确认项；不相关项跳过
- 不确定 API、依赖版本、文件、业务规则、配置或命令结果时，依次查项目实现与测试、相关文档和任务记录、锁定版本或官方文档；仍无法确认时询问用户，禁止把假设写成事实
- 新增功能或较大改动前，先检索相似实现（页面、布局、组件、样式资源和交互模式）；保持最小扰动，不做无关格式化、重构、依赖升级或方案替换
- 修 Bug 必须定位根因并验证原始复现场景；涉及跨层职责、数据流、权限、缓存、状态流转或持久化时，先判断架构边界
- 发现复用点、需求缺口、职责边界、验证缺口或风险时，按“必须处理 / 建议处理 / 暂不处理”说明；未经用户确认不得扩大范围
- 删除文件、删除目录、批量重构、资源清理等高风险操作，必须先获得明确确认

---

## 编码硬约束
- 所有源码、配置、文档统一使用 `UTF-8`；中文显示异常时，未确认是显示问题还是文件损坏前禁止覆盖
- 编写注释时统一使用中文，不使用纯英文注释
- PowerShell 命令不接受 `&&`，禁止使用；需要连续执行时改为分步执行或使用 PowerShell 兼容写法。

---

## 触发式协作规则
- 新功能、行为变更或用户使用 `/plan`：在形成计划前读取 `collaboration-AGENTS.md` 的「需求澄清与计划关卡」；纯问答、明确的低风险单点修改或用户要求直接执行时按其中的跳过条件处理
- 发现前提矛盾或更优思路、需要自行判断常规细节，或需要同步过程进展时：读取 `collaboration-AGENTS.md` 的「坦率反馈与过程沟通」
- 实际修改、`TASK.md` 维护、验证、交付、提交、回退、重复问题回顾或规则效果反馈：读取 `collaboration-AGENTS.md` 的对应章节
- 检测到其他 AI 会话并行操作同一工作区（`TASK.md` 出现非本会话条目、文件被会话外修改或输出目录被运行实例锁定）时：读取 `collaboration-AGENTS.md` 的「并行会话隔离」
- 只有用户明确要求提交时才可执行 `git commit`；“开始开发”“完成实现”“交付收口”等指令不等同于提交授权

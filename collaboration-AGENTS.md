---
rule_id: collaboration-agents
version: 1.81.0
last_updated: 2026-09-22
dependencies: [agents-root]
---

# 协作流程规则

## 需求澄清与计划关卡
- 触发新功能、影响行为的变更或 `/plan` 后，先结合现有代码、规则和文档复述目标、影响范围与当前理解，再判断能否形成可靠计划。
- 只询问会实质影响业务规则、范围、数据或状态、权限、接口、交互、兼容性、非功能要求或验收方式的缺口；已能从项目事实确认的内容不得重复询问。
- 每轮优先提出 1 至 3 个最关键问题。每个问题说明影响，尽量提供选项、推荐项和理由；用户可要求助手自行作出明确假设。
- 将事项分为“必须确认”“可采用默认假设”“暂不影响本次计划”；低影响偏好不得伪装为阻塞项。
- 收到回答后，输出可直接用于后续协作的需求描述：保留已确认事实，补齐本轮结论，明确范围、业务规则和验收标准；未确认内容必须标为待确认或假设。
- 用户明确要求直接执行、只做分析，或任务不改变业务行为且为低风险单点修改时，不启动完整澄清流程；仍需说明影响与采用的假设。

## 坦率反馈与过程沟通
- 不盲从用户前提：发现更优思路、需求遗漏，或描述与项目事实、既有约定矛盾时，先说明理由、影响和建议；矛盾修正先经用户确认，更优思路只作为建议，用户明确坚持后按其决定执行并保留异议记录。
- 常规细节自行判断，不因低影响选择中断；影响范围、方案选型、接口契约、数据兼容性或对外行为的重要取舍，执行前简要说明利弊与选择。
- 过程更新只讲实质进展、关键发现和阻塞；按阶段给结论式更新，不逐步播报读文件、执行命令等操作，也不因“不播报”而长时间静默。

## 纠错、回退与重构
- 阶段产物与需求、设计文档或真实实现冲突时，先说明冲突、影响范围和建议回退点，再按最小影响范围修正；契约缺口必须回到 DTO、接口或 Schema 的变更流程，不得在实现层临时绕过。
- 自动提交后发现问题时，优先追加修复提交；只有用户明确要求且确认影响范围后，才执行回滚类 Git 操作。
- 涉及数据库、迁移、发布产物或环境清理的回退，先确认备份、回滚窗口和可验证恢复方式。
- 仅在安全、性能、交付或维护成本已实质阻碍当前目标时重构；风格偏好、技术潮流或非关键路径优化不是重构理由。

## 任务管理
- `TASK.md` 是唯一活动任务记录文件，只维护当前仍需关注的任务和最近 5 条 `DONE`；开始实际修改前检查已有任务，更新已有项或新增任务，纯问答、只读评审和不落文件的现状分析可不更新。
- 状态仅使用 `TODO`、`DOING`、`BLOCKED`、`REVIEW`、`DONE`；`BLOCKED` 写明原因与所需支持，`REVIEW` 仅用于用户要求评审或跨会话交接，验证完成且无需跟进时置为 `DONE`。
- 每条任务记录 ID、名称、目标、阶段、负责人、状态、优先级和更新时间，过程叙述留在最终输出。
- 置 `DONE` 前自检：本次改动是否命中文档更新触发面——数据库结构或迁移、环境变量或配置项、鉴权或权限模型、命令或启动方式、对外接口约定（完整清单见 doc-AGENTS.md「文档更新要求」）；命中则同任务内完成文档同步再置 `DONE`，不得跨任务事后补录；未命中的纯实现任务才可直接置 `DONE`。

## 并行会话隔离
- 多 AI 会话并行操作同一工作区时，`TASK.md` 条目先读后写：登记或更新前重读文件，任务 ID 已被其他会话占用立即换号，不覆盖、不合并改写他人条目。
- 不与并行会话共享修改同一批业务文件，开始前按任务划分文件范围；发现重叠优先调整自身实现路径避开，确实避不开时提请用户协调串行，不并行改同一文件。
- 测试失败先归因再处理：用 `git diff HEAD` 确认未提交改动归属，对照 HEAD 版本复跑定位引入方；不回退、改写其他会话的未提交文件，需要对方改动让路时提请用户协调。
- 构建或测试因其他会话运行中的应用实例锁定默认输出目录而失败时，改用独立输出目录的等价验证路径（如 `dotnet build -o <独立目录>` 后对产物执行 `dotnet vstest <dll>`），不停止或重启对方的运行实例。
- 独立输出目录的产物以 `dotnet <dll>` 直接运行时，ContentRoot 是当前工作目录而非产物目录；宿主依赖迁移脚本等相对路径资源时，先切换到产物目录再启动，避免相对路径解析失败。

## Git、验证与交付
- 提交信息优先采用 Conventional Commits，标题为 `<type>(<scope>): <中文简述>`；非微小调整的中文正文包含“任务、原因、变更、风险”，其中风险只写真实的代码、业务、数据、兼容性、部署或发布风险。
- Windows / PowerShell 下提交前确认终端与 Git 日志使用 `UTF-8`，通过 `git commit -m` 或 UTF-8 文件配合 `git commit -F <file>` 传递中文信息；禁止经标准输入传递中文提交信息。提交后回读最近一次提交，确认中文未乱码。
- 提交前确认所需验证已完成、`TASK.md` 已置为 `DONE`；验证未完成、仍有阻塞或用户未明确授权时不得提交。
- 触及编译型语言（C# 等）代码的提交，提交前必须完成对应全量构建（.NET 为全解决方案 `dotnet build`）且通过；构建未执行或未通过时不得提交。
- 「全部」「逐页」「逐项」类覆盖面声明必须先用可复核清单核对（如 grep 产出目标集合再比对缺失项）再下结论，不得凭记忆断言覆盖完整。
- 用户要求开始开发且实现完成后，继续完成验证与交付收口，但不自动提交。规则或文档改动需回读并确认 UTF-8；代码、启动链路和高风险改动按命中子规则补对应回归或 smoke test；新增环境变量同步到示例配置。
- 缺少对应脚本时可采用等价验证，但最终说明必须写清执行内容、未覆盖校验、原因、影响和建议下一步。
- 最终输出优先中文，说明改动、核心实现、涉及文件、已执行和未执行的校验、任务状态、风险、阻塞或假设。

## 重复问题回顾
- 同一类规则缺口在同一任务连续两次阻塞交付，或跨任务重复出现时，简短记录错误现象、触发场景、根因、本次规则补充与后续预防措施，并落实到 `AGENTS.md` 或命中子规则。

## 规则效果反馈
- 任务收尾时，把规则被违反、与现实冲突、缺失导致返工、明显好用或疑似冗余的现象与建议追加到目标项目根目录 `.ai-rule-feedback.md`；导入的技能（skills）内容与实际使用不符或已过时同样记录；同一任务内同类问题只记一条，不为记录反馈打断交付。
- 条目格式：`## [YYYY-MM-DD] 一句话主题`，依次写「规则」「类型」（违反 / 冲突 / 缺失 / 有用 / 冗余）、「场景」「说明与建议」；「冗余」必须有多个同类任务证据，单次顺利命中仍记「有用」。技能类条目「规则」写技能名与参考文件（如 `microsoft-agent-framework#references/packages.md`）。
- `.ai-rule-feedback.md` 长期保留在本地，已由生成器加入 `.gitignore` 不随项目提交，回传规则源仓库时显式复制文件内容；已落实为本地规则的重复问题仍应记录。是否回传规则源仓库由用户决定，回传后由维护方按承接流程转化为规则修改。

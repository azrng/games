---
rule_id: doc-agents
version: 1.81.0
last_updated: 2026-09-22
dependencies: [agents-root]
---

# 文档规则

## 适用范围

- 作用域：需求文档、设计文档、契约说明、设计系统与开发文档维护
- 触发场景：涉及阶段 0 设计、契约说明、`design-system.yaml`、`doc/index.md`、`doc/requirements/` 或 `doc/design/` 时阅读

### 常见任务入口
- 新增功能方案：先看阶段 0 设计文档
- 更新字段、接口、DTO 或 schema：先看契约说明规范
- 更新设计系统、通用说明或文档结构：先看设计系统文件规范（如有）、文档更新要求与文档目录约定

---

### 阶段 0 — 设计文档（新功能强制前置）

**触发条件**：用户提出新功能需求

**执行方式**：由用户与 AI 对话协作产出，用户最终确认定稿

**产物**：当前业务模块/功能对应的 `doc/design/<module>/<feature>.md`，并关联已确认的 `doc/requirements/<module>/<feature>.md`；跨模块方案另记入 `doc/design/architecture.md`

**文档必须包含以下四个部分**：

| 部分 | 内容要求 |
| --- | --- |
| 需求追溯 | 关联 `REQ-` 编号、明确本次设计覆盖的范围和未覆盖项，不重复粘贴需求正文 |
| 契约引用（如涉及） | 关联已有 `CON-` 及最终事实来源，或关联阶段 0 创建的契约草案；不在设计文档中复制完整字段结构 |
| 技术设计 | 模块职责、数据流向、状态变化、技术决策和依赖边界 |
| 界面原型 | 每个页面的 ASCII 线框图，含关键状态（loading / empty / error） |
| 交互与异常 | 操作流程、状态流转、边界场景、错误处理方式和验证方式 |

**门控规则**：
- 用户明确确认对应的需求文档和设计文档后，才允许进入阶段 1
- AI 在此阶段只输出文档内容，不写任何实现代码

---

## 设计系统文件规范
`design-system.yaml` 是唯一设计系统文件，包含：
- 品牌与产品风格意图
- 语义化颜色 token
- 排版 token
- 间距、圆角、阴影 token
- 布局与断点规则
- Bootstrap 主题配置映射
- 页面状态规范
- 组件使用规则
- 明确的 `do / dont` 规则

---

## 主动建议规则
- 发现需求描述缺少角色、流程、字段、页面结构、权限、边界场景、交互说明、验收标准或非功能要求时，应主动补充为待确认项
- 发现实现方案与设计文档、接口契约、需求基准或设计系统不一致时，应主动提醒并建议同步修订
- 维护文档时优先更新已有事实来源，不新增平行说明；确需新增文件时，应说明新增原因和归档位置
- 对不确定的业务事实、接口字段和运行方式，只能标记为待确认或引用已验证来源，禁止写成确定结论
---

## 文档更新要求
以下内容发生变化时，必须同步更新相关文档：
- 命令、脚本或启动方式
- 环境变量
- 配置项
- 构建、发布或部署流程
- 数据库结构、迁移或初始化方式
- 对外使用方式或接口约定

文档同步是命中上述清单的任务的完成前置条件：置 `DONE` 前先比对本次改动，命中则在同一任务内更新文档，不得跨任务事后补录。

## 契约生命周期

- 已存在的接口、DTO、Schema 或事件契约：若尚无 `CON-` 编号，先在 `doc/contracts/<module>.md` 增加只含编号、来源路径、状态和用途的索引项；设计文档只引用 `CON-` 编号、最终事实来源路径和本次使用范围，不复制完整字段结构。
- 新增契约时，阶段 0 先创建 `doc/contracts/<module>/<feature>.md` 作为契约草案，标注稳定的 `CON-` 编号和 `status: draft`；完整请求、响应或事件字段只维护在该草案中，不写入需求或设计正文。
- 阶段 0 的设计文档必须引用对应的 `CON-` 编号，并明确契约是已有事实来源还是待落地草案；不能把尚不存在的 DTO、Schema 或 Swagger 文档写成已确认事实。
- 用户确认需求、设计和新增契约草案后，才允许进入阶段 1。阶段 1 将草案落地为项目 DTO、Schema、接口或事件定义后，代码 / Schema 成为最终事实来源；契约草案只保留来源链接和变更记录，并标记为 `superseded`，不得继续维护第二份完整字段表。
- 阶段 1 或后续实现发现契约缺口时，先回到契约变更流程；不得在实现层临时补字段或让前端、调用方私自约定未记录的结构。

- 优先更新对应业务模块/功能的已有需求、设计和契约文档，不新增重复说明。
- 新增功能时，先定位或创建 `doc/requirements/<module>/<feature>.md`，再创建对应的 `doc/design/<module>/<feature>.md`；两者使用稳定 ID 互相引用，并同步更新 `doc/index.md`、`doc/requirements/index.md` 和 `doc/design/index.md`。
- 需求文档发生业务规则、范围、权限或验收标准变化时，必须检查对应设计文档和实现是否需要同步调整；设计变更不得反向覆盖未经确认的需求事实。
- 设计文档发生技术方案、状态、接口调用或错误处理变化时，只更新设计文档和必要的契约/实现说明，不把技术细节复制回需求文档。
- 跨模块变化更新 `doc/design/architecture.md` 或明确的跨模块需求文档；模块文档通过链接引用，不复制公共段落。
- 文档拆分或迁移时，保留原文档中的有效内容，更新索引和关联链接；旧兼容文件迁移完成后只保留说明，不再追加新章节。

### 文档目录约定

需求文档和设计文档按业务模块拆分，不按 Controller、Service、Repository 等技术层拆分。模块下继续按功能拆分，使同一功能的需求、设计和契约可以通过稳定 ID 互相追溯。

```text
doc/
├── index.md                         # 文档总索引，只做导航
├── requirements/
│   ├── index.md                     # 需求索引与项目级目标
│   └── <module>/<feature>.md        # 业务需求
├── design/
│   ├── index.md                     # 设计索引
│   ├── architecture.md              # 跨模块架构设计
│   ├── <module>/<feature>.md        # 功能设计
│   └── prototype/                   # 可选：高保真界面原型（HTML）
└── contracts/
    ├── <module>.md                  # 可选的跨模块契约索引
    └── <module>/<feature>.md        # 新契约草案或契约补充说明
```

- `doc/index.md`：列出模块、功能、文档状态、文档路径和关联关系；内容必须轻量，不能替代具体文档
- `doc/requirements/index.md`：维护项目目标、范围和模块目录；详细业务需求放在模块/功能文件中
- `doc/requirements/<module>/<feature>.md`：只描述为什么做、给谁用、做什么、业务规则、权限、边界和验收标准，不写技术实现方案
- `doc/design/index.md`：维护设计文档导航和整体设计状态
- `doc/design/architecture.md`：只记录跨模块架构、公共数据流、公共技术决策和跨模块约束
- `doc/design/<module>/<feature>.md`：只描述对应需求的实现方案、模块职责、数据流、状态、交互、异常和技术决策；通过需求 ID 引用需求，不重复粘贴完整需求
- `doc/design/prototype/`：可选目录，仅当界面原型需要升级为高保真原型时才创建；存放 HTML 高保真图，按 `<module>/<feature>.html` 与功能设计文档对应，并在对应设计文档中互相链接；默认仍使用设计文档内的 ASCII 线框图，不强制生成高保真图
- `doc/contracts/<module>.md`：仅在需要给多个模块提供契约索引或补充说明时使用；`doc/contracts/<module>/<feature>.md` 用于阶段 0 的新契约草案。DTO、Schema、公开接口定义仍以代码或共享 Schema 为最终事实来源
- 新文档在 YAML front matter 中至少标注 `doc_id`、`doc_type`、`module`、`status`、`last_updated` 和关联文档路径；需求文档使用 `REQ-` ID，设计文档使用 `DES-` ID，契约草案使用 `CON-` ID
- `status` 统一使用 `draft`、`in-review`、`approved`、`superseded`；只有对应需求、设计和新增契约草案均为 `approved` 时，才允许进入阶段 1。已批准文档发生业务或技术变更时，先改为 `in-review`，确认后再恢复为 `approved`
- 新功能应同时创建同模块的需求文档和设计文档，例如 `doc/requirements/user/login.md` 与 `doc/design/user/login.md`
- 新项目或多模块项目直接使用上述结构；单模块小项目仅对存量未迁移文档允许暂时保留单文件，新功能仍必须使用上述需求 / 设计拆分结构。文档超过约 300 行、包含多个业务模块，或任务通常只需要其中一部分时，必须迁移拆分
- 旧项目中的 `doc/requirement.md` 和 `doc/design/设计文档.md` 视为兼容入口。迁移时按业务模块和功能切分原有章节，更新索引；迁移完成后旧文件只保留迁移说明和链接，不再追加新内容

### requirement 要求
- 新需求文档放在 `doc/requirements/<module>/<feature>.md`；`doc/requirements/index.md` 只维护项目级目标、模块清单和文档索引。
- 每个需求文档必须有稳定的 `REQ-` 编号，并至少覆盖：项目背景、目标与范围、用户角色、页面、接口或模块清单、核心业务流程、关键业务对象及字段含义、权限与可见范围、边界场景与异常、非功能要求、验收标准、待确认项。
- 需求文档只描述业务事实、用户价值和可验证结果；关键字段只写业务含义、取值规则和验收语义，不写模块如何分层、使用什么框架或请求 / 响应 / 事件的完整字段结构。
- 需求跨多个模块时，在需求索引中说明影响范围；各模块文档只保留本模块规则，公共约束放在 `doc/requirements/index.md` 或独立的跨模块需求文档中。
- 设计文档不得复制完整需求内容；用 `REQ-` 编号建立追溯关系，需求变更后再同步更新对应设计文档。
- 旧 `doc/requirement.md` 仅作为兼容入口。若它仍是模板或已明显膨胀，先按模块/功能迁移，再继续补充真实需求，不要继续向旧单文件追加新章节。

- 若项目没有需求文档，AI 必须先创建 `doc/index.md`、`doc/requirements/index.md` 和当前业务模块下的 `doc/requirements/<module>/<feature>.md`，根据用户当前输入生成需求初稿，再进入对应设计文档整理；有界面时补充页面范围，没有界面时写接口 / 命令 / 模块范围。
- 自动生成的需求文档至少应包含：业务目标、用户角色、页面（如有）或接口 / 模块范围、核心流程、关键业务字段含义、权限规则、边界场景、验收标准、已知约束和稳定的 `REQ-` 编号。
- 若当前项目仍只有 `doc/requirement.md`，先判断本次需求所属模块；只修改对应拆分文件，或在确认迁移范围后将旧文件按模块切分，不把新功能继续无限追加到旧单文件。
- 若用户输入不足以支撑完整需求，AI 必须显式写出“待确认项”与“当前假设”，可以先形成初稿，但不得将假设内容表述为已确认事实。

### 回顾分析记录格式
- 适用场景：同类问题因规则缺口在同一任务中连续两次阻塞交付，或跨任务重复出现后，触发简短回顾分析时使用
- 内容保持简短，建议至少包含：
  - 错误现象
  - 触发场景
  - 根因
  - 本次补充或修改的规则
  - 后续如何避免或验证不再复发

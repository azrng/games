# 游戏平台设计文档

## 五子棋移动端独立页面优化（2026-05-13）

### 架构设计
- 页面结构：保留 `gomoku-advanced/index.html` 作为 PC 页面，新增 `gomoku-advanced/mobile.html` 作为移动端专用页面。
- 数据流向：移动端和 PC 端共用 `gomoku-advanced/script.js` 与 `gomoku-advanced/ai-worker.js`，避免复制五子棋规则、AI 通信和棋盘渲染逻辑。
- 入口策略：首页游戏清单为五子棋补充 `mobilePath`，外层 `js/app.js` 根据视口宽度选择 PC 或移动端页面加载到 iframe。
- 组件与交互：移动端页面使用顶部状态栏、棋盘主舞台、底部固定操作栏和设置抽屉；设置控件沿用原有 id，保证共享脚本可直接绑定。

### 功能需求
- 页面列表：
  - PC 页面：`gomoku-advanced/index.html`，保持现有双栏布局。
  - 移动端页面：`gomoku-advanced/mobile.html`，面向手机首屏体验。
- 功能点：
  - 开始、悔棋、认输可在移动端底部操作栏直接使用，移动端不展示提示入口。
  - 当前状态、步数、当前回合展示在顶部状态栏。
  - 电脑先手、难度、主题、禁手规则移动到设置抽屉。
  - 横竖屏或窗口尺寸变化时，棋盘自动重新计算尺寸。
- 业务规则：
  - 不改变五子棋规则、AI 算法、禁手判定与 PC 端提示逻辑。
  - 移动端触摸落子必须与视觉交叉点一致。
- 权限矩阵：本项目为静态游戏页，无登录权限区分，所有用户可访问全部五子棋功能。

### 界面原型

```text
移动端五子棋
+--------------------------------+
| 五子棋            设置  状态    |
| 当前回合 / 步数 / AI 状态       |
+--------------------------------+
|                                |
|        15 x 15 棋盘             |
|     轻触交叉点完成落子          |
|                                |
+--------------------------------+
| 开始 | 悔棋 | 认输              |
+--------------------------------+

设置抽屉
+--------------------------------+
| 对局设置                    关闭 |
| [ ] 电脑先手                    |
| 难度    [简单 v]                |
| 主题    [经典 v]                |
| [ ] 禁手规则                    |
+--------------------------------+
```

- loading：棋盘区域显示 `AI 思考中...` 遮罩。
- empty：未开始时顶部状态展示“点击‘开始’进入游戏”。
- error：Worker 或脚本异常时由浏览器控制台暴露，当前静态页不新增错误页。

### 交互说明
- 用户在首页进入五子棋时，若视口宽度小于等于 640px，加载 `mobile.html`；否则加载 `index.html`。
- 点击“设置”打开底部抽屉，再次点击遮罩、关闭按钮或抽屉内“完成”关闭。
- 点击电脑先手和禁手规则的开关滑块即可切换；点击“开始”时脚本会读取当前电脑先手和难度值，禁手规则变更后立即影响棋盘禁手标记与落子校验。
- 点击棋盘时，脚本以 canvas 实际显示矩形计算落子坐标，避免 CSS 缩放造成偏移。
- 点击“开始”后进入游戏，AI 思考期间禁用相关按钮并显示加载遮罩。
- 窗口 resize 时首页会按当前视口切换五子棋 PC / 移动端页面；游戏内棋盘同步重新计算尺寸。

## 迷宫冒险布局与主题优化（2026-05-13）

### 架构设计
- 页面结构：保留 `maze/index.html` 单文件游戏实现，继续使用原有递归回溯迷宫生成、键盘移动、通关结算和桌面端限制逻辑。
- 布局策略：新增 `game-sidebar` 作为桌面端固定侧栏，整合玩法说明与关卡、步数、耗时、尺寸、模式统计；`game-container` 独立作为迷宫舞台。
- 主题策略：迷宫页面统一到游戏平台浅色体系，使用蓝色作为主操作色、绿色作为探索/已访问状态、琥珀色作为终点提示，避免原深蓝霓虹主题与其他游戏割裂。
- 入口文案：统一清单和说明文档改为桌面端专用，不再宣称移动端方向键和缩放。

### 功能需求
- 保持三档难度、关卡递增、路径访问痕迹、通关进入下一关等玩法不变。
- 迷宫主体需要避让左侧侧栏，避免 HUD 和统计浮层遮挡可视区域。
- 难度选择和通关弹层使用浅色面板，与首页和五子棋的视觉语言保持一致。
- 移动设备访问时继续展示桌面端专用提示。

### 界面原型

```text
----------------------+--------------------------------+
| 迷宫冒险             |                                |
| 穿过迷宫，找到伙伴   |          迷宫舞台               |
| 目标说明             |     玩家 / 路径 / 终点          |
|                      |                                |
| 关卡  步数           |                                |
| 耗时  尺寸           |                                |
| 模式                 |                                |
+----------------------+--------------------------------+
```

### 交互说明
- 玩家在难度弹层选择简单、中等、困难后开始游戏。
- 游戏中使用方向键或 `WASD` 移动，统计区实时更新关卡、步数、耗时、尺寸与模式。
- 侧栏占用的屏幕区域会参与安全区域计算，迷宫自动居中到剩余舞台空间。
- 通关弹层展示本关耗时和步数，点击按钮进入下一关。

## 2048 游戏进度存储（2026-05-13）

### 架构设计
- 复用现有 `AppStore`（`window.AppStore`）存储工具，通过 `setGameJSON`/`getGameJSON` 操作 localStorage，key 为 `game_2048_game_state`。
- 仅修改 `2048/script.js`，不新增文件。

### 功能需求
- 每次移动成功后，将棋盘状态（grid、score、over、won、keepPlayingAfterWin）保存到 localStorage。
- 页面加载时，优先从 localStorage 恢复存档；无有效存档或游戏已结束时开始新游戏。
- 游戏结束（Game Over 或胜利未继续）不保存进度，下次打开重新开始。
- 点击"新游戏"时清除已保存的状态。

### 交互说明
- 用户无需手动操作，进度自动保存和恢复。
- 关闭页面再打开，棋盘、分数恢复到上一步操作后的状态。

## 色觉挑战（Kuku Kube）（2026-05-13）

### 架构设计
- 页面结构：新增 `color-challenge/index.html` 作为纯移动端单页面游戏入口，配套 `style.css` 和 `script.js`。
- 数据流向：页面启动后自动生成第一轮，游戏状态保存在内存中；最高分和最长存活轮数写入 `localStorage`，键名前缀使用需求文档指定的 `color-challenge-`。
- 渲染方式：使用 CSS Grid 和 `background-color` 渲染色块，不使用 Canvas；脚本暴露 `window.ColorChallenge` 中的少量纯函数，便于 smoke test 校验难度与存储规则。
- 首页接入：在 `data/games.js` 中新增色觉挑战条目，移动端和桌面端均指向同一入口；页面自身在桌面宽度展示移动端提示。

### 功能需求
- 进入页面自动开局，无需点击开始。
- 每轮生成一个 N×N 色块网格，只有一个色块颜色不同。
- 轮次 1-5 使用 2×2，6-10 使用 3×3，11-20 使用 4×4，21-30 使用 5×5，31 轮起使用 6×6。
- 正确点击后得分 +1 并进入下一轮；错误点击后结束游戏并展示本局得分、存活轮数、历史最高分和最长轮数。
- 色差随轮次递增逐渐缩小，但保留最小可辨识下限。
- 快速连续点击时每轮只响应第一次判定。

### 界面原型

```text
+----------------------------------+
| 色觉挑战                  12秒    |
| 轮次 得分 最高分 最长轮数         |
+----------------------------------+
|                                  |
|          色块 CSS Grid            |
|     找出唯一不同色并轻触          |
|                                  |
+----------------------------------+
| 纯色觉游戏可能不适合所有用户       |
+----------------------------------+
```

### 交互说明
- 玩家点击正确色块后，色块短暂缩放高亮，再平滑进入下一轮。
- 玩家点击错误色块后，网格抖动并弹出结算面板。
- 点击“再来一局”清空当前状态并从第 1 轮重新开始。
- 横竖屏切换时网格通过 CSS `aspect-ratio` 保持正方形并适配可用宽度。

## 数字华容道（Sliding Puzzle）（2026-05-13）

### 架构设计
- 页面结构：新增 `sliding-puzzle/index.html` 作为移动端单页面游戏入口，配套 `style.css` 和 `script.js`。
- 数据流向：棋盘状态、步数、计时和撤销栈保存在页面内存；每种规格的历史最少步数与最短用时写入 `localStorage`，键名前缀为 `sliding-puzzle-`。
- 生成策略：从目标完成状态出发，随机执行合法移动进行逆向打乱，避免生成无解局面；3×3、4×4、5×5 分别使用不同打乱步数。
- 渲染方式：使用 CSS Grid 渲染棋盘，数字方块为按钮，空位为占位格；脚本暴露 `window.SlidingPuzzle` 中的纯函数用于 smoke test 校验移动、可解性、打乱和记录。
- 首页接入：在 `data/games.js` 中新增数字华容道条目，移动端和桌面端均指向同一入口；页面自身在桌面宽度展示移动端提示。

### 功能需求
- 首次进入展示规格选择，支持 3×3 入门、4×4 经典、5×5 挑战。
- 选择规格后自动打乱并开始游戏，计时从第一步有效移动开始。
- 与空位相邻的方块可点击或触摸滑动进入空位；不相邻方块无响应。
- 每次有效移动步数 +1，并将移动前棋盘压入撤销栈。
- 点击“撤销”回退一步，撤销到初始打乱状态后按钮禁用。
- 点击“打乱”重新打乱当前规格；点击“重置”确认后返回规格选择。
- 棋盘归位后展示通关结算，包含规格、步数、用时、历史最少步数和最短用时。

### 界面原型

```text
+----------------------------------+
| 数字华容道              规格      |
| 规格  步数  时间                  |
| 最少步数 / 最短用时               |
+----------------------------------+
|                                  |
|       N x N 数字滑块棋盘          |
|                                  |
+----------------------------------+
|  撤销  |  打乱  |  重置           |
+----------------------------------+
```

### 交互说明
- 规格选择面板默认展示，选择后进入游戏。
- 点击相邻方块会自动滑入空位；触摸滑动超过阈值时同样尝试移动该方块。
- 重置当前棋盘前使用浏览器确认框，避免误触。
- 通关后可选择“再来一局”沿用当前规格，或“换个规格”回到规格选择。

## 找猫咪（Hidden Cats）优化（2026-05-13）

### 架构设计
- 页面结构：保留 `hidden-cats/index.html` 单页实现，继续使用 Canvas 2D 程序化绘制素描场景、猫咪和干扰物。
- 数据流向：关卡、猫咪位置、已找到状态、点击次数、提示次数、缩放和平移状态保存在页面内存；历史最高关卡写入 `localStorage`，键名前缀为 `hidden-cats-`。
- 渲染策略：每关生成随机场景种子，由地标建筑、斜塔、街区、云、飞行物、人物、餐桌、密集窗格、短线纹理和猫形干扰物组合出一张黑白旅行线稿；猫咪绘制为灰黑小猫轮廓，不使用高亮色，并从场景藏点池中抽取位置。
- 首页接入：在 `data/games.js` 中注册 `hidden-cats`，移动端入口指向 `hidden-cats/index.html`。

### 功能需求
- 页面启动后自动生成第 1 关，并从画面展示开始计时。
- 每关猫咪数量从 3 只开始，每 3 关增加 1 只，上限 10 只；画布尺寸随关卡递增，每关都会重新随机组合线稿场景与藏点。
- 支持单指平移、双指缩放、点击命中判定、提示定位、重置缩放、重新开始和下一关。
- 命中猫咪后更新进度并重绘，所有猫咪找到后展示通关弹层，记录历史最高关卡。
- 未命中时在点击位置显示短暂错误标记，避免用户误以为操作无效。

### 界面原型

```text
+----------------------------------+
| 找猫咪              点击  时间    |
| 第 1 关                          |
+----------------------------------+
| 已找到猫咪              1 / 3     |
| ======= 进度条 ================= |
+----------------------------------+
|                                  |
|     随机黑白线稿大场景 + 线稿猫咪 |
|    可平移、缩放、点击寻找猫咪     |
|                                  |
+----------------------------------+
|  提示  |  重置缩放  |  重新开始  |
+----------------------------------+
```

### 交互说明
- 触摸移动距离超过阈值时视为平移，不触发点击命中；短按视为点击。
- 双指缩放以双指中心为锚点，并限制在最小 / 最大缩放范围内。
- 视口边界会限制平移范围，避免玩家把画布完全拖出可视区。
- 提示会把视图放大并平移到一只未找到猫咪附近，支持玩家继续放大指定区域观察细节。
- 桌面调试时支持鼠标点击同一套命中判定，便于 smoke test 和本地验证。
- 关卡重开或进入下一关时会先停止旧计时器，再重置状态并启动新计时。

## 动物翻翻棋（DouShou Qi – Flip Card）（2026-06-09）

### 架构设计
- 页面结构：新增 `flip-chess/index.html` 作为双人对战单页面游戏入口，配套 `style.css` 和 `script.js`。
- 数据流向：棋盘状态（16张牌的位置、正反面、归属）、当前回合方、游戏阶段保存在页面内存；对局记录（胜负次数）写入 `localStorage`，键名前缀为 `flip-chess-`。
- 渲染方式：使用 CSS Grid 渲染 4×4 棋盘，每张牌为一个可点击的卡牌元素，背面使用统一花纹，正面显示动物 emoji + 等级标识。翻牌使用 CSS 3D transform 翻转动画。
- 游戏阶段管理：使用有限状态机管理 `init` → `rps`（猜拳）→ `playing` → `gameover` 四个阶段。
- 首页接入：在 `data/games.js` 中新增翻翻棋条目，移动端和桌面端均指向同一入口。

### 功能需求

#### 棋子设定
- 共 16 张牌：8 红方 + 8 蓝方，每方各 8 种动物各 1 张。
- 等级从高到低：象(8)、狮(7)、虎(6)、豹(5)、狼(4)、狗(3)、猫(2)、鼠(1)。
- 特殊规则：鼠可以吃象（唯一越级克制），同级互吃（同归于尽）。

#### 棋盘与布局
- 4×4 方格，共 16 个格子。
- 所有牌背面朝上，随机洗牌后均匀放置到 16 个格子中。
- 不区分阵营初始位置，牌的归属在玩家第一次翻到时确定。

#### 先手决定
- 游戏开始时进入猜拳阶段，双方各选石头/剪刀/布，系统判定胜者获得先手。
- 猜拳 UI 为左右对称布局：左侧红方选择区、右侧蓝方选择区，中间显示结果。
- 猜拳平局时自动重新进行，直到分出胜负。

#### 游戏主循环
每回合玩家二选一操作：
1. **翻牌**：点击一张背面朝上的牌，牌翻开展示动物和颜色。翻牌后回合立即结束，轮到对方。
2. **移动**：点击一张已翻开的己方牌，再点击相邻（上下左右，不含对角线）的空格或敌方牌。移动后回合结束，轮到对方。

#### 战斗判定（移动时触发）
- 目标格为空格：直接移动，无战斗。
- 目标格为敌方牌：
  - 己方等级 ≥ 敌方等级 → 己方牌移动到该格，敌方牌被吃掉（从棋盘移除）。
  - 己方等级 < 敌方等级 → 不允许移动，需重新选择。
  - 特殊：鼠(1) vs 象(8) → 鼠胜，象被吃掉。
  - 同级 → 同归于尽，双方牌均从棋盘移除。
- 目标格为己方牌 → 不允许移动。

#### 胜利条件
- 吃光对方所有棋子 → 获胜。
- 对方无任何合法操作（无暗牌可翻且无合法移动）→ 获胜。

### 界面原型

```text
猜拳阶段
+----------------------------------+
|         动物翻翻棋               |
|         决定先手                  |
+----------------------------------+
|                                  |
|   红方            蓝方           |
|  ┌──┐            ┌──┐          |
|  │✊│            │✊│          |
|  └──┘            └──┘          |
|  ┌──┐            ┌──┐          |
|  │✌️│            │✌️│          |
|  └──┘            └──┘          |
|  ┌──┐            ┌──┐          |
|  │🖐│            │🖐│          |
|  └──┘            └──┘          |
|                                  |
|      请双方同时选择              |
+----------------------------------+

游戏中状态
+----------------------------------+
| 动物翻翻棋       红方回合         |
+----------------------------------+
|                                  |
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │ ?  │ │ 🐱 │ │ ?  │ │ ?  │  |
|  │    │ │ 红 │ │    │ │    │  |
|  └────┘ └────┘ └────┘ └────┘  |
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │ ?  │ │ ?  │ │ 🐺 │ │ ?  │  |
|  │    │ │    │ │ 蓝 │ │    │  |
|  └────┘ └────┘ └────┘ └────┘  |
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │ ?  │ │ 🐘 │ │ ?  │ │ ?  │  |
|  │    │ │ 红 │ │    │ │    │  |
|  └────┘ └────┘ └────┘ └────┘  |
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │ ?  │ │ ?  │ │ ?  │ │ 🐶 │  |
|  │    │ │    │ │    │ │ 蓝 │  |
|  └────┘ └────┘ └────┘ └────┘  |
|                                  |
+----------------------------------+
|  红方剩余: 7  蓝方剩余: 7        |
|  暗牌: 12                        |
+----------------------------------+

选中己方牌后的可移动位置高亮
+----------------------------------+
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │    │ │ 🐱 │ │ ██ │ │    │  │
|  │    │ │ 红 │ │可移│ │    │  │
|  └────┘ └────┘ └────┘ └────┘  |
|  ┌────┐ ┌────┐ ┌────┐ ┌────┐  |
|  │    │ │ ██ │ │    │ │    │  │
|  │    │ │可移│ │    │ │    │  │
|  └────┘ └────┘ └────┘ └────┘  |
+----------------------------------+

游戏结束弹层
+----------------------------------+
|          游戏结束                 |
|                                  |
|        红方获胜！                 |
|                                  |
|     红方剩余：5 张               |
|     蓝方剩余：0 张               |
|     总回合数：24                  |
|                                  |
|   [ 再来一局 ]  [ 返回首页 ]     |
+----------------------------------+
```

### 交互说明
- **猜拳阶段**：双方各自点击石头/剪刀/布按钮，双方都选择后显示结果，胜者先手。平局自动重来。
- **翻牌操作**：当前回合方点击任意暗牌，牌以 CSS 3D 翻转动画翻开，显示动物和阵营颜色。翻牌后回合自动结束。
- **移动操作**：
  1. 点击己方已翻开的牌 → 牌高亮选中，可移动的相邻格高亮显示（绿色=空格可移动，红色=可吃敌方，黄色=同归于尽）。
  2. 点击高亮格 → 执行移动/战斗，动画播放后回合结束。
  3. 点击非高亮格或再次点击己方牌 → 取消选中。
- **战斗动画**：吃子时播放攻击方移向防守方的过渡动画，被吃方缩小消失。同归于尽时双方同时消失。
- **回合提示**：顶部状态栏显示当前回合方（红/蓝），用对应颜色高亮。
- **胜负弹层**：游戏结束时弹出结算面板，显示获胜方、双方剩余牌数、总回合数。
- **再来一局**：清空所有状态，重新进入猜拳阶段。

### 边界场景与异常
| 场景 | 处理方式 |
|------|----------|
| 翻到己方牌 | 回合直接结束，轮到对方 |
| 翻到敌方牌 | 回合直接结束，轮到对方（不能立即战斗，需下回合移动） |
| 选中己方牌但无可移动目标 | 选中后无高亮格提示，点击其他位置取消选中 |
| 己方牌被相邻敌方更高等级威胁 | 无特殊处理，玩家自行判断是否移动 |
| 所有暗牌翻开后 | 进入纯移动阶段，双方只能移动已翻开的己方牌 |
| 一方无暗牌且无合法移动 | 判定该方失败 |
| 快速连续点击 | 操作锁定，动画播放期间不接受新输入 |
| 猜拳同时选择 | 双方选择后统一判定，平局自动重来 |

### 非功能要求
- 页面首屏加载 < 1s（纯静态资源，无外部依赖）。
- 翻牌动画使用 CSS 3D transform，时长 300ms，流畅无卡顿。
- 棋盘使用 CSS Grid 渲染，格子为正方形，响应式适配不同屏幕。
- 移动端触控目标最小 44×44px。
- 支持 iOS Safari 和 Android Chrome 最近两个主版本。
- 视口设置 `width=device-width, initial-scale=1.0, user-scalable=no`。

### 待确认项
- [ ] 动物 emoji 是否满足视觉需求，还是需要自绘 SVG 图标。
- [ ] 是否需要音效（翻牌音、吃子音、胜利音）。
- [ ] 是否增加人机 AI 模式（V2）。
- [ ] 是否增加计时模式（每回合限时）。
- [ ] 是否需要皮肤系统（卡通/复古手绘风格切换）。

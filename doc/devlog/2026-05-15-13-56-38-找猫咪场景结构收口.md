# 找猫咪场景结构收口

## 本次目标

- 收口新增场景后的结构重复和主题一致性问题。
- 提升背景缓存稳定性、障碍物主题匹配、decoy 投放合理性和场景辨识度。

## 核心改动

- 将 `drawSceneBackground` 改为 `SCENE_DRAWERS` lookup 分发，并统一调用纹理层。
- `buildBackgroundCache` 增加 `try/finally`，确保离屏 canvas 绘制异常时能恢复主 canvas 和 context。
- 新增 `SCENE_OBSTACLES`，按场景筛选障碍物类型。
- 新增 `SCENE_DECOY_REGIONS`，限制假猫按场景几何区域投放。
- 新增花园、厨房、博物馆专属纹理函数，强化不同线稿品类的视觉手感。
- 花园鸟屋位置加入 seed random jitter。
- 更新 smoke test 覆盖场景分发、缓存保护、主题障碍物、decoy 区域和专属纹理入口。

## 修改文件

- `hidden-cats/index.html`
- `hidden-cats/smoke.test.js`
- `TASK.md`
- `doc/devlog/2026-05-15-13-56-38-找猫咪场景结构收口.md`

## 校验情况

- 已执行：`node hidden-cats\smoke.test.js`
- 已执行：`git diff --check`
- 仓库未发现 package 级构建脚本，本次未执行构建。

## 风险或遗留项

- 未执行真实浏览器截图回归。
- 场景绘制仍在单文件中，后续应拆分模块。
